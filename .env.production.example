# ===================================================
# Silicon River 前端生产环境配置模板
# ===================================================
#
# 使用说明：
# 1. 复制此文件为 .env（不要提交到 Git）
# 2. 根据你的后端部署方式配置 VITE_API_BASE
# 3. 在 1Panel 中上传或直接在界面配置环境变量
#
# ===================================================

# ---------------------------------------------------
# 后端 API 地址配置
# ---------------------------------------------------
# 重要：此变量决定前端如何调用后端接口
#
# 方案 A：后端有独立域名（推荐）
# VITE_API_BASE=https://api.yourdomain.com
#
# 方案 B：后端通过 IP 和端口暴露
# VITE_API_BASE=http://123.45.67.89:8000
#
# 方案 C：后端在内网（与前端服务器在同一 VPC）
# VITE_API_BASE=http://172.16.0.10:8000
#
# 方案 D：前端和后端共用域名（通过反向代理）
# VITE_API_BASE=（留空，使用相对路径）
# 此时需要在前端 Nginx 中配置反向代理到后端

VITE_API_BASE=

# ---------------------------------------------------
# 示例配置
# ---------------------------------------------------
# 假设你的后端服务器 A 地址是 api.backend-server.com
# VITE_API_BASE=https://api.backend-server.com

# 或者使用 IP 地址（不推荐用于生产）
# VITE_API_BASE=http://123.45.67.89:8000
